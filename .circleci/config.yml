version: 2.1

# Define the jobs we want to run for this project
jobs:
  deploy:
    docker:
      - image: node:latest
    steps:
      - checkout
      - run: echo "$(pwd)"
      - run: ls -la
      - run: ssh -i "test_ec2_linux.pem" ubuntu@ec2-52-27-176-232.us-west-2.compute.amazonaws.com
      - run: ls -la

# Orchestrate our job run sequence
workflows:
  deployer:
    jobs:
      - deploy